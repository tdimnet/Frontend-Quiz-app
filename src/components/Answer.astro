---
interface Props {
    content: string
    number: string
}

const { content, number } = Astro.props
---

<li class="answer-btn">
    <span class="answer-number">{number}</span>
    <span class="answer-content">{content}</span>
</li>

<style>
    li {
        background-color: #FFF;
        padding: 12px;
        border-radius: 12px;
        cursor: pointer;
        transition: .4s;
        border: 1px solid #FFF;
        display: flex;
        align-items: center;
        font-size: 18px;

        &:hover {
            border: 1px solid #A729F5;
        }

        .answer-number {
            color: #626C7F;
            background-color: #F4F6FA;
            height: 40px;
            width: 40px;
            display: flex;
            align-items: center;
            justify-content: center;
            margin-right: 16px;
        }

        .answer-content {
            color: #3B4D66;
        }
    }
</style>
